/**
 * Convenience hook for accessing auth state and actions.
 * Thin wrapper around the Zustand auth store for cleaner component usage.
 */
export { useAuthStore as useAuth } from '@/auth/auth.store';
