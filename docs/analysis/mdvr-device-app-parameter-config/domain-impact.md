# Domain impact

- No new bounded context: stays in **device command / telemetry** domain.
- Introduce a **presentation catalog** of Meitrack events for Parameter Alarm UI (shared package or fleet-management export).
- Do not conflate with notification-service Alarm Rules.
