# Residual questions (technical — non-blocking to start Phase 1A spike)

1. Prefer **server composer** endpoint vs client multi-send? (Analysis recommends server.)
2. Host under `/commands?tab=parameter` vs `/devices/:id/parameter`?
3. Confirm Meitrack PDF section for **delay recording** and **digital input** event codes used by your MD300 firmware build.

Answer during spike or with “Proceed to implementation”.
