# Implementation plan

1. **Product answers** (`questions.md`) — block  
2. Slice A — UI honesty + driver detail device links  
3. Slice B — assignment history schema + APIs + UI timeline  
4. Slice C — work summary aggregation + report fix  
5. Slice D — alarm `driverId` stamp + filter + driver alarms list  
6. Tests: assign closes interval; work sum across two vehicles; alarm attribution at boundary  

**Do not implement until status → approved.**
